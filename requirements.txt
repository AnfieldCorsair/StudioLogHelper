PyQt6>=6.6
# Optional but recommended for 3-5x faster JSON parsing
orjson>=3.9
# Optional: better markdown rendering (C extension)
markdown>=3.6
# For tests
pytest>=8
pytest-qt>=4.4
# Streaming JSON parser dependency
ijson>=3.1
